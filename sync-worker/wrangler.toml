name = "zhiti-sync"
main = "worker.js"
compatibility_date = "2025-01-01"

# 部署前先执行：
#   npx wrangler kv namespace create SYNC_KV
# 把输出里的 id 填到下面，并设置你的同步口令：
[[kv_namespaces]]
binding = "SYNC_KV"
id = "b49bc68fee224a20a4370ccb4eeaac37"

[vars]
SYNC_TOKEN = "my-secret-token-123"
